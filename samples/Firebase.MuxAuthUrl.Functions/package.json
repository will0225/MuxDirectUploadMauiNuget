{
  "name": "firebase-mux-auth-url-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase Function sample to issue Mux direct upload URLs",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^5.1.1"
  }
}
