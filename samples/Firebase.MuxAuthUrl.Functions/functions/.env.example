# Copy to .env and set your dedicated Firestore database ID for Mux webhook docs only.
# Create that database in Firebase Console → Firestore → Databases → Create database.
# Leave unset to use the (default) database (not recommended if that DB holds live app data).
FIRESTORE_MUX_DATABASE_ID=mux-webhook
